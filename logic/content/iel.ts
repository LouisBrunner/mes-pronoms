import {PronounsStorage} from 'logic/storage/types'
import {SamePlural} from 'logic/types'

export const IEL: PronounsStorage = {
  pronouns: {
    PronomSujet: {
      singular: 0,
      plural: SamePlural,
    },
  }
}
